/**
 * Skills marquee shown on the Home hero (task 3.3).
 * Technical terms / proper nouns are identical in ES and EN, so this is a
 * plain string list rather than a Bilingual[] — no translation needed.
 * Every term is traceable to the CV "AI Transformation Lead" — see the
 * "Skills (marquee)" section appended to docs/content-sources.md.
 */
export const skills: string[] = ['LLMs', 'RAG', 'Agents', 'Evals', 'LangGraph', 'LLMOps', 'FastAPI', 'AWS'];
